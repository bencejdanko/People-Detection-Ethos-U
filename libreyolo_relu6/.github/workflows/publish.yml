name: Publish to PyPI

on:
  push:
    tags:
      - "v*"

jobs:
  verify-release-source:
    name: Verify release source
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Require tag commit on release
        env:
          RELEASE_BRANCH: release
        run: |
          git fetch origin "${RELEASE_BRANCH}:refs/remotes/origin/${RELEASE_BRANCH}"
          TAG_COMMIT="$(git rev-parse HEAD)"
          if ! git merge-base --is-ancestor "$TAG_COMMIT" "origin/${RELEASE_BRANCH}"; then
            echo "::error::Release tags must point to a commit reachable from origin/${RELEASE_BRANCH}."
            exit 1
          fi

  build:
    name: Build package
    needs: verify-release-source
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v4

      - name: Build sdist and wheel
        run: uv build --out-dir dist/

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/

  publish-pypi:
    name: Publish to PyPI
    needs: build
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
